#include "main.h"

/**
* _abs - computes the absolute value of an integer
* @n: value of an integer
*
* Return: return 0
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
